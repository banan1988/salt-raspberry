developer:
  tools:
    git.install:
      version: latest
      package-args: "/NoGuiHereIntegration"
      config:
        path: "%USERPROFILE%\\.gitconfig"
        content:
          [core]
              autocrlf = false
          [user]
              name=banan
              email=banan@b2d.com.pl
          [gui]
              encoding = utf-8
              tabsize = 4

    jq:
      version: latest
    gradle:
      version: latest
    maven:
      version: latest
    docker-desktop:
      version: latest
